{
  "name": "CSP Crawl Tier 2 — Apify RAG Web Browser",
  "meta": {
    "description": "Tier 2 site crawl: Apify RAG Web Browser (~$0.001/page). Handles JS-rendered SPAs. Probes homepage plus 8 high-value page paths directly (pricing, features, integrations, customers, case-studies, about). maxCrawlPages: 10. Part of M74 three-tier crawl architecture."
  },
  "nodes": [
    {
      "id": "webhook-tier2",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "webhookId": "csp-crawl-tier2-rag",
      "position": [240, 300],
      "parameters": {
        "path": "csp-crawl-tier2-rag",
        "httpMethod": "POST",
        "responseMode": "onReceived",
        "options": {}
      }
    },
    {
      "id": "validate-input",
      "name": "Validate Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300],
      "parameters": {
        "jsCode": "const raw = $input.first().json;\nconst body = raw.body || raw;\nconst website = (body.website || '').trim();\nif (!website) throw new Error('website is required');\nconst apify_token = (body.apify_token || process.env.APIFY_API_TOKEN || '').trim();\nif (!apify_token) throw new Error('apify_token is required');\nconst supabase_url = (body.supabase_url || process.env.SUPABASE_URL || '').trim();\nconst supabase_key = (body.supabase_key || process.env.SUPABASE_KEY || '').trim();\nconst base = website.replace(/\\/+$/, '');\nreturn [{ json: { website, vendor_name: body.vendor_name || '', apify_token, supabase_url, supabase_key, base } }];"
      }
    },
    {
      "id": "run-rag-browser",
      "name": "Run RAG Web Browser",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [680, 300],
      "parameters": {
        "method": "POST",
        "url": "=https://api.apify.com/v2/acts/apify~rag-web-browser/run-sync-get-dataset-items?token={{ $json.apify_token }}&timeout=90",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ startUrls: [ { url: $json.website }, { url: $json.base + '/pricing' }, { url: $json.base + '/plans' }, { url: $json.base + '/pricing-plans' }, { url: $json.base + '/features' }, { url: $json.base + '/product' }, { url: $json.base + '/platform' }, { url: $json.base + '/integrations' }, { url: $json.base + '/customers' }, { url: $json.base + '/case-studies' }, { url: $json.base + '/about' } ], maxCrawlPages: 10 }) }}",
        "options": { "timeout": 120000 }
      }
    },
    {
      "id": "extract-name-text",
      "name": "Extract Name + Text",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [900, 300],
      "parameters": {
        "jsCode": "const website = $('Validate Input').first().json.website;\nconst inputName = $('Validate Input').first().json.vendor_name || '';\nconst results = $input.first().json;\nconst items = Array.isArray(results) ? results : [results];\n\n// Classify each page by URL path\nfunction classifyPage(url) {\n  const u = (url || '').toLowerCase();\n  if (/\\/pric|\\/plans|\\/pricing-plans/.test(u)) return 'PRICING PAGE';\n  if (/\\/feature|\\/platform|\\/product/.test(u)) return 'FEATURES PAGE';\n  if (/\\/integrat|\\/marketplace|\\/apps/.test(u)) return 'INTEGRATIONS PAGE';\n  if (/\\/customer|\\/case-stud|\\/success-stor/.test(u)) return 'CUSTOMERS PAGE';\n  if (/\\/about|\\/company|\\/team/.test(u)) return 'ABOUT PAGE';\n  if (/\\/solution|\\/use-case/.test(u)) return 'SOLUTIONS PAGE';\n  if (/\\/securit|\\/trust|\\/compliance/.test(u)) return 'SECURITY PAGE';\n  if (/\\/contact|\\/demo/.test(u)) return 'CONTACT PAGE';\n  return null;\n}\n\n// Split into homepage (first unclassified) and typed pages\nlet homepageItem = null;\nconst typedPages = [];\nfor (const item of items) {\n  const pageUrl = item.url || item.metadata?.url || '';\n  const label = classifyPage(pageUrl);\n  if (!homepageItem && !label) {\n    homepageItem = item;\n  } else if (label) {\n    typedPages.push({ item, label });\n  }\n}\nif (!homepageItem) homepageItem = items[0] || {};\n\nconst rawTitle = homepageItem.metadata?.title || homepageItem.title || '';\nconst homepageText = homepageItem.text || homepageItem.markdown || '';\n\n// Build labelled sections for typed pages\nconst sections = typedPages\n  .map(({ item, label }) => {\n    const t = (item.text || item.markdown || '').trim();\n    return t.length > 50 ? `\\n\\n[${label}]\\n${t}` : '';\n  })\n  .filter(Boolean)\n  .join('');\n\nconst pricing_page_found = typedPages.some(p => p.label === 'PRICING PAGE' && (p.item.text || p.item.markdown || '').length > 100);\n\nconst TAGLINE_SUFFIXES = new Set(['analytics','automation','dashboard','insights','intelligence','management','monitoring','platform','reporting','software','solution','solutions','tracking']);\nfunction isTagline(text) {\n  const lower = text.toLowerCase();\n  const last = lower.split(/\\s+/).pop();\n  if (TAGLINE_SUFFIXES.has(last)) return true;\n  if (/\\bai[- ]powered\\b|\\b\\w+-powered\\b|\\bpowered by\\b/.test(lower)) return true;\n  if (/\\b\\d{4}\\b/.test(lower)) return true;\n  return false;\n}\nfunction cleanCandidate(raw) {\n  let s = (raw || '').replace(/<[^>]+>/g,' ').replace(/\\s+/g,' ').trim();\n  if (!s) return '';\n  for (const sep of [' | ',' - ',' \u2013 ',' \u2014 ',': ']) {\n    if (s.includes(sep)) {\n      for (const part of s.split(sep)) {\n        const c = cleanCandidate(part);\n        if (c) return c;\n      }\n    }\n  }\n  if (isTagline(s)) return '';\n  if (s.split(/\\s+/).length > 5) return '';\n  if (s.length > 40) return '';\n  return s;\n}\nfunction domainStem(url) {\n  try {\n    const base = new URL(url).hostname.replace(/^www\\./, '').split('.')[0].toLowerCase();\n    for (const p of ['get','try','use','my','the']) {\n      if (base.startsWith(p) && base.length - p.length >= 4)\n        return base.slice(p.length).charAt(0).toUpperCase() + base.slice(p.length+1);\n    }\n    return base.charAt(0).toUpperCase() + base.slice(1);\n  } catch { return ''; }\n}\n\nconst name = cleanCandidate(rawTitle) || cleanCandidate(inputName) || domainStem(website);\nconst combined = homepageText + sections;\nconst word_count = combined ? combined.split(/\\s+/).length : 0;\n\nreturn [{ json: { website, name, word_count, text: combined.slice(0, 15000), tier_used: 'tier2_rag', ok: word_count > 0, pricing_page_found, pages_fetched: items.length } }];"
      }
    },
    {
      "id": "store-pages-tier2",
      "name": "Store Pages",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [1120, 300],
      "parameters": {
        "method": "POST",
        "url": "={{ $('Validate Input').first().json.supabase_url + '/rest/v1/vendor_pages?on_conflict=vendor_website,page_url' }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $('Validate Input').first().json.supabase_key }}" },
            { "name": "Authorization", "value": "=Bearer {{ $('Validate Input').first().json.supabase_key }}" },
            { "name": "Prefer", "value": "resolution=merge-duplicates" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ [{ vendor_website: $('Extract Name + Text').first().json.website, page_url: $('Extract Name + Text').first().json.website, title: $('Extract Name + Text').first().json.name, clean_text: $('Extract Name + Text').first().json.text, word_count: $('Extract Name + Text').first().json.word_count, page_depth: 0, tier_used: 'tier2_rag' }] }}",
        "options": {
          "timeout": 15000,
          "response": { "response": { "neverError": true } }
        }
      }
    },
    {
      "id": "store-crawl-result-tier2",
      "name": "Store Crawl Result",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [1340, 300],
      "parameters": {
        "method": "PATCH",
        "url": "={{ $('Validate Input').first().json.supabase_url + '/rest/v1/cs_vendors?website=eq.' + encodeURIComponent($('Validate Input').first().json.website) }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $('Validate Input').first().json.supabase_key }}" },
            { "name": "Authorization", "value": "=Bearer {{ $('Validate Input').first().json.supabase_key }}" },
            { "name": "Prefer", "value": "return=minimal" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { crawl_tier2_result: { ok: $('Extract Name + Text').first().json.ok, pipeline: 'csp-crawl-tier2-rag', crawled_at: new Date().toISOString(), pages_fetched: $('Extract Name + Text').first().json.pages_fetched, word_count: $('Extract Name + Text').first().json.word_count, pricing_page_found: $('Extract Name + Text').first().json.pricing_page_found, fields: { name: $('Extract Name + Text').first().json.name } } } }}",
        "options": {
          "timeout": 15000,
          "response": { "response": { "neverError": true } }
        }
      }
    }
  ],
  "connections": {
    "Webhook": { "main": [[{ "node": "Validate Input", "type": "main", "index": 0 }]] },
    "Validate Input": { "main": [[{ "node": "Run RAG Web Browser", "type": "main", "index": 0 }]] },
    "Run RAG Web Browser": { "main": [[{ "node": "Extract Name + Text", "type": "main", "index": 0 }]] },
    "Extract Name + Text": { "main": [[{ "node": "Store Pages", "type": "main", "index": 0 }]] },
    "Store Pages": { "main": [[{ "node": "Store Crawl Result", "type": "main", "index": 0 }]] },
    "Store Crawl Result": { "main": [[]] }
  },
  "settings": { "executionOrder": "v1", "saveManualExecutions": true },
  "staticData": null,
  "pinData": {}
}
